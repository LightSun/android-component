

int resultCode -> LogicResult(int code, Object data).
add shareResult(flags) flags = FLAG_SHARE_TO_NEXT. FLAG_SHARE_TO_POOL
  (put result to pool ?)
manager.getPerformResult(int key). (manage pool)

//resultConvertor. transform result data.


parallel tasks 支持分布式，容错机制(其余的完成，然后回调错误装 和 已经执行正确的结果)。
support retry?
fork, join ?
